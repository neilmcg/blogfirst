<% form_for([@post, @comment]) do |f| %>
<p>
	<%= f.label :email %><br />
	<%= f.text_field :email %>
</p>
<p>
	<%= f.label :body %><br />
	<%= f.text_area :body %>
</p>
<p class="button"><%= f.submit "Submit" %></p>
<% end %>